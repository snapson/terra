extends layout

block content
	h1= title
	p Welcome to #{title}
	h3#cpu Here will be CPU usage for N(30 sec by default) time
	

	| <script src="http://code.jquery.com/jquery-2.1.4.min.js"></script>
	| <script src="http://cdn.socket.io/socket.io-1.3.0.js"></script>
	| <script>
	| 	var socket = io.connect('http://localhost:5353');
	|	var default_time = 30000;
	| 	socket.on('CPU', function (data) {
	|		$('#cpu').html( data.cpu );
	|		socket.emit('cpu-event', { time: 100 });
	| 	});
	| </script>
